#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# 运行 lint-staged
pnpm lint-staged

# 类型检查
echo "🔍 Type checking..."
pnpm type-check

echo "✅ Pre-commit checks passed!"
