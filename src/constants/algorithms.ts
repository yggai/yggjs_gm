/**
 * 算法常量定义
 *
 * @description 国密算法相关的常量定义
 */

/**
 * SM3 哈希长度 (字节)
 */
export const SM3_DIGEST_LENGTH = 32;

/**
 * SM4 密钥长度 (字节)
 */
export const SM4_KEY_LENGTH = 16;

/**
 * SM4 块大小 (字节)
 */
export const SM4_BLOCK_SIZE = 16;

/**
 * SM2 私钥长度 (字节)
 */
export const SM2_PRIVATE_KEY_LENGTH = 32;

/**
 * SM2 公钥长度 (字节，未压缩格式)
 */
export const SM2_PUBLIC_KEY_LENGTH = 64;
